# 🧮 Calculadora Científica em C

## 📌 Descrição

Projeto desenvolvido para praticar lógica de programação utilizando a linguagem C.  
A aplicação funciona via terminal e permite realizar operações matemáticas com validação de erros e execução contínua até que o usuário decida encerrar.

O projeto foi construído com foco em organização, uso de funções e tratamento de entradas inválidas.

---

## 🚀 Funcionalidades

- Adição (`+`)
- Subtração (`-`)
- Multiplicação (`*`)
- Divisão (`/`)
- Exponenciação (`e`)
- Raiz quadrada (`r`)
- Estrutura de repetição com `do-while`
- Tratamento de:
  - Operador inválido
  - Divisão por zero
  - Raiz de número negativo

---

## 🛠 Tecnologias Utilizadas

- Linguagem C
- Biblioteca padrão `<stdio.h>`
- Biblioteca matemática `<math.h>`

---

## 🧠 Conceitos Aplicados

- Funções
- Estrutura `switch-case`
- Estruturas condicionais (`if / else if`)
- Estrutura de repetição (`do-while`)
- Validação de dados de entrada
- Separação entre lógica de cálculo e controle do programa